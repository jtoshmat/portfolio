<?php
//$parms = json_encode($ids);
//pid = prospect_id; ids = id of each responsed row in prospect_responses table; uid = questions form row id for each questions/reponses
//$parms = array($responses);
//header('Content-type: application/json');
//$parms = json_encode($ids);

//if (!empty($parms)){
//echo json_encode($parms);
//}

 
?>
